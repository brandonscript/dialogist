/**
 * Default dialog paper radius for the Next.js demo app, applied once via
 * {@link DialogProvider} `defaultOptions` in `ClientProviders.tsx`.
 * Per-`open()` overrides still work (e.g. live radius in the high-frequency sync demo).
 */
export const DEMO_DIALOG_BORDER_RADIUS = 16;
