/** Must match `useLocalStorage` key in `demo/nextjs/src/contexts/DemoStateContext.tsx`. */
export const DEMO_FULLSCREEN_STORAGE_KEY = "dialogist-demo-fullscreen";

/** Sandbox hero; stable selector for “app is up” (see `Sandbox.tsx` `#sandbox-header`). */
export const DEMO_SANDBOX_HEADER = "#sandbox-header";
